name: deploy-docsv3

on:
  workflow_dispatch:
  pull_request:

jobs:
  deploy-docsv3:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run setup script
        run: |
          ./setup.sh
