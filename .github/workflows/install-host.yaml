name: install-host

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'install.sh'
      - '.github/scripts/tests/test_install_docker_capabilities.sh'
      - '.github/workflows/install-host.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  install-host:
    name: install-host (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test Docker capability convergence
        run: bash .github/scripts/tests/test_install_docker_capabilities.sh

      - name: Install host dependencies from official packages
        # Force installation only on this disposable runner so the test cannot
        # pass solely because GitHub's preinstalled Docker happens to work.
        run: |
          sudo env CI=true OPENADKIT_CI_FORCE_DOCKER_INSTALL=true \
            ./install.sh --no-nvidia --verify

      - name: Verify installed Docker capabilities
        run: |
          set -euo pipefail
          docker --version
          docker compose version
          docker buildx version
          sudo docker info > /dev/null
          (
            cd deployments/scenario-simulation
            docker compose \
              --env-file ../base/base.env \
              --env-file scenario-simulation.env \
              config -q
          )
