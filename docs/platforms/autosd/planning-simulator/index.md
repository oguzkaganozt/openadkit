# AutoSD + Planning + Simulator

This folder shows how to run and test scenarios with containers managed by systemd and Podman.

## Folder Structure

* `aib`: automotive-image-builder manifest files to build an AutoSD image
* `components`: quadlet files to run Open AD Kit inside a container in an AutoSD image
* `docs`: extra documentation files, such as architecture diagrams
